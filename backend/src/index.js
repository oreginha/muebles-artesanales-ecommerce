module.exports = {
  register(/* { strapi } */) {
    // registrar funciones personalizadas
  },

  bootstrap({ strapi }) {
    // función que se ejecuta al iniciar Strapi
    console.log('🚀 Muebles Artesanales API iniciada correctamente');
  },
};
